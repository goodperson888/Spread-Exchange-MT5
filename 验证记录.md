# 本机 MT5 集成验证 · 2026-09-17

- 当前开发机器：macOS；MT5 原生连接目标：Windows 64 位。
- 23 项 Python 测试通过：包括 MT5 连接/品种/权限检查、本机 API 安全、配置和数量验证、纸面自动开平仓、跨配置行情拒绝、动态 USDT/USD 配对身份、费率/非 USDT 佣金换算、未知成交不重复提交、第二腿拒单立即撤回 MT5，以及 MT5 平仓拒绝时不错误平掉币安对冲腿。
- 所有测试凭据、账号、终端对象使用隔离临时目录与替身，没有发送真实订单。
- JavaScript 语法检查、Python 编译检查通过。
- 浏览器已验证新页面、仅本机服务、币安公开 Bid/Ask、USDT/USD 资产指数、本地模拟 MT5 Bid/Ask、实时入场/退出价差和折线图。已验证实盘/纸面与止盈规则的条件显示。
- 当前 macOS 验证网络未收到币安 WebSocket 帧，程序已正确回退 REST 并持续重连；WebSocket 实时流须在最终 Windows 网络环境再验收。
- Windows 打包依赖版本已从 PyPI 核对；提供构建、单元测试和包内组件导入自检脚本。
- 尚未执行 Windows 打包，尚无已验收 exe；尚未验证真实 Windows 终端、实际账户权限及盘口。替身测试不能证明实机连通性。
- 纸面自动交易引擎和实盘适配代码已补全；验证过程没有提交任何真实订单。

## 2026-09-18：持仓对账展示

- 实盘对账返回当前连接品种的币安持仓、币安未成交委托及 MT5 持仓快照；对账不一致时仍可展示已读取的数据。MT5 挂单不在本次读取范围。
- 展示平台开仓均价、数量、MT5 开仓时间及本策略归属。快照随手动对账更新，不是实时持仓推送；纸面模式不读取真实持仓。
- 外部仓位不会自动加入交易组或自动平仓。币安持仓均价可能合并多笔成交，未实现外部历史成交配对、历史汇率还原或策略接管。
- 本程序交易组显示记录中的开仓成交价差：币安成交价 × 该笔记录的 USDT/USD − MT5 成交价；两边成交不一定同时发生。
- 43 项 Python 测试通过，新增 4 项覆盖外部仓位只读展示、对账失败保留明细、读取异常撤销旧对账结果、纸面模式不读取真实账户。浏览器使用拦截的测试数据验证，不发送真实交易请求。

### 对账按钮“未找到”修复

- 对账、启动及暂停按钮此前未传请求体，被公共请求函数作为 GET 发送；后端仅提供 POST 路由，因此返回 404，未执行对账。现改为携带空 JSON 的 POST 请求。
- 44 项 Python 测试通过，包含实际 HTTP 路由的 GET 拒绝与 POST 分发验证；2 项 Node 测试执行真实前端按钮处理函数，校验请求方式、启动/暂停切换及对账失败提示。全部使用替身，不连接交易账户。
- Windows 云构建增加前端按钮测试，避免仅验证后端或忽略请求方式的页面替身再次漏检。未做真实账户成交验证。

## 2026-09-18：已有持仓独立接管篮子

- 完成配对预览、持仓再次核验、确认登记、幂等重试、独立启停与退出条件。确认只落盘和对账，不发单；重启/重连暂停管理。
- 保留币安汇总均价和 MT5 各票据原始开仓价，历史汇率和累计费用须显式确认。原始成本不清零，历史费用不冒充平台已复核；程序接管后发出的平仓费用另行复核。
- 仅支持无未平策略组时，将所选完整 MT5 多头与当前品种全部币安空头配平。挂单、数量不匹配、过期/变化预览、无历史成本确认均不能接管。旧仓不补仓，与新策略独立，占用总手数上限。
- 原生 MT5 平仓核验原票据、持仓标识、方向、原始 magic、开仓时间和价格；逐票平仓，按 MT5 实际成交数量减少币安对应敞口。人工改仓会撤销对账状态并暂停接管管理。
- 59 项 Python 测试、3 项 Node 按钮测试通过；浏览器验证选择→预览→修改使预览失效→确认→独立启停及轮询保留输入，所有 API 均被隔离替身拦截，未向真实平台下单。移动端管理表单已截图检查。
- 尚未做 Windows 真实 MT5/币安账户的接管及实际成交验收。历史成本与分摊费用仍属于估算。
