# 黄金双边自动交易：AI 交接文档

更新时间：2026-09-22（Asia/Shanghai）  
项目目录：`/Users/laochen/Documents/912/策略研究_2026-09-14/黄金双边自动交易`  
当前分支：`main`  
当前提交：`77dfb84 Add configurable Binance-first paired entry`

## 1. 当前阶段判断

项目已经完成从“策略研究/纸面验证”到“可在目标电脑上进行实机验收”的主要开发工作，但还没有达到可以无条件承诺实盘安全的阶段。当前最准确的阶段名称是：

> **核心功能完成，进入 Windows + MT5 + 币安真实环境联调和小额纸上/实盘前验收阶段。**

目前可以继续做：

- 在 macOS 上用 MT5 MCP 读取真实行情，运行纸面交易和图表验证；
- 在 Windows 上连接原生 MT5、币安正式 API，验证账户检查、EA 行情推送、对账和小额订单链路；
- 将实际成交延迟、滑点、拒单、时间同步和费用复核结果记录下来。

目前不能宣称已经验证：

- Windows 目标电脑上的真实 MT5 终端稳定运行；
- EA 在目标经纪商上长期推送不丢行情；
- 币安和 MT5 的真实成交质量、滑点和断线恢复；
- 两个平台原子成交（这个能力本身无法由两个独立平台保证）。

## 2. 产品目标和边界

目标是一个仅运行在用户本机的黄金双边配对执行程序：

- 币安 USDⓈ-M 永续：`XAUUSDT`，策略腿固定为卖出/回补空头；
- MT5 黄金：例如 `XAUUSD`、`XAUUSDm`，策略腿固定为买入/卖出多头；
- 入场价差：`币安 Bid × USDT/USD - MT5 Ask`；
- 退出价差：`币安 Ask × USDT/USD - MT5 Bid`；
- 服务只监听本机 `127.0.0.1:8766`，不提供局域网访问；
- MT5 EA 行情推送接收端使用本机 `127.0.0.1:8767`；
- Mac 只适合开发、纸面交易和 MCP 只读行情；真实 MT5 下单目标是 Windows 原生终端。

项目和旧的“黄金差价监控”程序分开，旧程序通常使用 `8765`，本项目使用 `8766`。

## 3. 主要运行方式

### macOS 开发/纸面

```bash
python3 launcher.py
```

页面打开后选择 MT5 MCP 真实行情、纸面模式，检查 MT5，连接双边行情，再启动纸面自动开仓。MCP 只读，不调用下单或平仓。

### Windows 目标机

用户需要自行安装、登录并保持运行经纪商的 MT5 终端。构建包内包含 Python 运行环境、MetaTrader5 组件、页面和 EA 文件，但不包含 MT5 终端本身。

```powershell
powershell -ExecutionPolicy Bypass -File build_windows.ps1
```

接收者必须解压完整目录后运行 `GoldPairLocal.exe`，不能只复制 exe。Windows 云构建 workflow 是手动触发，构建摘要提供 zip 下载链接。

## 4. 当前架构

| 文件 | 作用 |
|---|---|
| `launcher.py` | 启动本机服务、准备数据目录、打开浏览器 |
| `server.py` | HTTP/SSE API、配置、连接检查、对账、接管、后台运行时 |
| `trading_engine.py` | 双腿执行、交易组、异常撤回、止盈止损、网格、收益 |
| `trading_brokers.py` | 纸面/实盘 broker 抽象与订单调用 |
| `binance_connector.py` | 币安 REST、签名、账户、订单、资金费、手续费、时间同步 |
| `mt5_connector.py` | Windows 原生 MT5 进程与账户/行情/订单接口 |
| `mt5_mcp.py` | Mac MCP 只读适配器 |
| `market_push.py` | MT5 EA Socket 推送接收、身份和报价校验 |
| `mt5/GoldPairQuotes.mq5` | 只读 MT5 EA 源码；只推报价，不下单 |
| `quote-stream.js` | 浏览器 SSE 图表流、断线重连、历史补发 |
| `trading-app.js` | 页面状态、策略控制、订单/收益/图表渲染 |
| `trading_store.py` | 本机 SQLite 交易日志和状态持久化 |
| `position_adoption.py` | 已有持仓读取、接管预览、登记与校验 |
| `entry_preflight.py` | 临近阈值时币安时间、权限和预检 |
| `config.example.json` | 默认配置和字段示例 |
| `README.md` | 面向使用者的操作说明 |
| `验证记录.md` | 阶段性测试和真实验收记录 |

## 5. 已实现功能

### 行情和图表

- 币安 `bookTicker` WebSocket 长连接，断线自动重连，REST 回退；
- MT5 优先使用 EA Socket 推送，EA 不可用时保留轮询兼容；
- SSE 图表长连接，隐藏页面时暂停，恢复后补发或重新加载历史；
- 高频采样不可被后续采样改写；最近 24 小时高频保留，更早按分钟归档 30 天；
- 图表绘制和策略线程分离，浏览器卡顿不应直接停止后台执行线程；
- 页面同时显示报价年龄、本机收到时间差和原始时间差；
- MT5 经纪商服务器时间与币安 UTC 的时区差不会参与交易有效性判断。交易校验使用 `observed_ms`，原始时间只用于诊断。

### 配置和数量

- 自动读取 MT5 账户、服务器、品种、合约大小、最小手数、手数步长；
- 自动读取币安合约过滤器、最小数量、步长、名义金额等；
- MT5 手数按 0.01 等规则校验，币安数量按 XAU 数量规则校验；
- 自动计算配平关系，例如 `MT5 0.01 手 ↔ 币安 1 XAU`；
- API Key、Secret、MCP Token 保存到本机配置并可清除；不写入交易日志；
- 配置采用原子替换，普通页面输入有浏览器草稿恢复机制。

### 策略和执行

- 每组手数、开仓阈值、退出收窄、目标退出价差、最低净收益；
- 逐组退出或整篮子退出；最低净收益可以要求同时满足；
- 网格补仓：`开仓阈值 + 网格间距 × 补仓次数`，受最大组数和总手数上限限制；
- 新增“暂停新开仓”：暂停只阻止新组，已有仓位继续止盈、止损和异常处理；
- 默认 `entry_leg=binance`，先发送币安开仓，再发送 MT5 开仓；仍支持切换为 MT5 优先；
- 平仓配对部分按 MT5 实际已平数量减少币安腿，避免单边扩大；
- 普通滑点预算和保护性平仓滑点预算分开；保护性平仓默认预算更宽；
- 订单意图先写 SQLite，再发送；未知回报先查询，不盲目重发；
- 一边拒单、部分成交、状态未知或行情过期时，暂停新开仓并撤回已成交敞口；
- 不承诺跨平台原子成交，只保证状态不明时不假设成功、不重复提交。

### 实盘安全门

- 实盘必须使用 Windows 原生 MT5，Mac MCP 不允许实盘启动；
- 连接后要完成币安账户检查、MT5 检查、双边规格校验和持仓对账；
- 币安单向/双向持仓均做了读取和对账适配，策略只管理自己的空头腿；
- 账户模式、品种、数量、未完成委托、MT5 票据或 Magic 不一致时禁止自动处理；
- 币安时间同步失败、报价过期、双边本机时间差超限时不新开仓；
- 可选已有持仓接管篮子：接管只登记、不下单，确认后还需单独启动旧仓管理；
- 接管旧仓不补仓、不与新策略篮子合并，不支持只接管部分币安空头。

### 费用和收益

- USDT/USD 可自动读取币安指数，暂时失败时保留上次值并告警；
- 币安 taker 费率读取账户接口，成交后用实际 commission 覆盖预估；
- 币安资金费实盘按已入账的 `FUNDING_FEE` 读取并分配；
- MT5 Swap 实盘读取持仓/成交记录；MT5 佣金首笔成交前只能页面预估，成交后以 deal 复核；
- 持仓组实时净收益会随行情变化，平仓后用实际成交、手续费、资金费和 Swap 固定；
- 未完成复核的收益明确标记为估算。

## 6. 当前最重要的行为约定

1. 触发价差只是信号，不是成交价差。第一腿成交后会重新读取行情，订单流水同时记录监控价差、实际成交价差和偏移。
2. 两腿完整成交但实际价差低于触发阈值时，不立即反向平仓，交易组按实际成交价差继续管理，避免额外手续费往返。
3. 单边成交时，程序优先消除单边敞口；不会为了修正显示价差而开反向新组。
4. Binance 订单 `-1021`、网络超时和未知回报都按“状态未知”处理，先查询账户/订单/成交，不直接重发。
5. `250 ms` 是轮询兼容间隔，不是成交保证；接入 EA 后由新报价事件唤醒策略，但真实成交仍受网络、撮合和串行下单影响。
6. Binance 与 MT5 不能做到真正的跨平台原子成交；必须接受极短时间的单边敞口和保护性处理。

## 7. 已知问题、风险和未完成验收

### 必须在 Windows 目标环境验收

- 原生 MT5 终端与真实经纪商账户连接；
- EA 挂载、`.set` 参数加载、`127.0.0.1` 访问权限和 `8767` Socket 推送；
- EA 推送报价年龄是否长期稳定，尤其 MT5 报价年龄是否经常超过 500–1000ms；
- 币安代理、API 白名单、时间同步、WebSocket 和 REST 实际延迟；
- 小额纸面/极小金额实盘的双腿成交、拒单、部分成交、回报和复核；
- Windows 睡眠、网络断开、MT5 重启、浏览器关闭后的后台行为；
- 真实账户持仓模式、手续费、资金费和 MT5 Swap 数值。

### 设计限制

- 两个平台无法原子成交；
- MT5 Python API 没有通用 tick WebSocket，EA 只推行情，不负责交易；
- 交易策略执行串行化，订单等待期间不允许无界并发下单；
- 真实 MT5 佣金率通常不能在首笔成交前自动可靠获得；
- 历史平台数据不能还原为过去每个时刻的真实可成交双边价差，图表历史主要是本机运行期间采样；
- 旧仓接管的原始成本、历史汇率和历史费用可能只能由用户确认估算。

### 当前代码/文档注意事项

- 工作树有一个未提交的 `trading-app.js` 修改：已将异常保护处理中、保护完成、自动开仓暂停分开显示，避免旧的“正在保护性平仓”文案残留。
- `README.md` 的成交异常章节仍需复核是否完全反映最新的“币安优先入场”配置；以 `config.example.json` 的 `execution.entry_leg` 和 `trading_engine.py` 为准。
- 最近用户截图中的“原始时间差约 3 小时”是 MT5 经纪商服务器时区与币安 UTC 的差异；真正执行相关的是本机收到时间差和报价年龄。
- 当前页面还没有完整的独立“网络诊断”面板；已有连接状态、WebSocket/EA 状态、报价年龄、本机时间差、错误和重连信息。后续可加入 Binance REST RTT、WebSocket 最后心跳、MT5 Socket 最后报价、断线次数和时间同步结果，并以此控制“禁止新开仓、继续平仓”。
- 当前页面改动未重新生成 Windows zip；需要重新运行 GitHub Actions 的手动 Windows 构建，才能让其他电脑拿到最新包。

## 8. 验证现状

截至本交接文档生成前：

- Python：`python3 -m unittest discover -s tests -q`，83 项通过；
- Node：`node --test tests/*.test.cjs`，13 项通过；
- `node --check trading-app.js` 通过；
- `git diff --check` 通过；
- 测试使用替身和隔离临时目录，没有发送真实订单；
- 还没有在目标 Windows 电脑完成完整真实验收。

## 9. 建议后续工作顺序

### P0：实机安全验收

1. 提交当前 `trading-app.js` 状态文案修复。
2. 更新 README 中的默认入场顺序说明。
3. Windows 构建并下载完整 zip，确认 exe 自检通过。
4. 目标 Windows 安装并登录 MT5，挂载 EA，验证 EA Socket 推送。
5. 币安使用关闭提币权限、仅必要合约权限的 API Key，并确认固定出口 IP 白名单。
6. 先纸面运行，保存报价年龄、MT5/币安 RTT、本机收到时间差和重连记录。
7. 再用极小数量验证开仓、拒单、保护性平仓、正常平仓和费用复核。

### P1：网络和执行可观测性

- 增加网络诊断卡片和明确阈值；
- 日志记录 Binance REST RTT、服务器时间偏移、订单请求/回报耗时；
- 日志记录 EA Socket 最近心跳、最近报价、断线重连和 MT5 报价年龄；
- 将“禁止新开仓”与“继续平仓/风控”状态分开展示；
- 为状态机增加前端测试：保护处理中、保护完成、对账等待、恢复可启动。

### P2：文档和运维

- 更新 `README.md` 和 `验证记录.md` 到当前 2026-09-22 版本；
- 在构建产物中附带一页 Windows 验收清单；
- 记录每台电脑的 MT5 服务器时区、网络代理、EA 推送延迟和实际成交偏移；
- 将网络诊断结果与交易组/订单流水关联，方便事故复盘。

## 10. 交给下一位 AI 的工作方式

先读取本文件、`README.md`、`验证记录.md`，再查看 `git status` 和最近提交。不要把 `data/`、API Key、Secret、MCP Token、`quote-push.json` 或真实交易日志提交到 Git。修改执行逻辑前先读对应测试；修改状态机后至少运行 Python 83 项和 Node 13 项测试。涉及实盘行为时，优先保持“未知不重发、先对账、暂停新开仓但继续处理已有敞口”的安全原则。
